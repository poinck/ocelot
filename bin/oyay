#!/bin/bash

if [[ -f ~/.oyay ]] ; then
    . ~/.oyay
    if [[ -n "$oyay" ]] ; then
        OYAY="$oyay"
        rm ~/.oyay
    else
        OYAY=$1
    fi
else
    OYAY=$1
fi

vurl=$( youtube-dl -f mp4 -g "$OYAY" )
omxplayer -o both "$vurl"

