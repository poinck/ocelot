# set screen_width, this is still required for dzen2, depricated
#screen_width=1366

# setting the ocelot_path is required if you do not install executables of
# "ocelot" in a directory inside $PATH
ocelot_path="/home/username/gits/ocelot/"

# we love wallpapers, even if we cant see them all the time,
# default: "${ocelot_path}/desktop.png"
#wallpaper="/home/username/path/to/desktop.jpg"

# lockscreen pictures are also a good thing,
# default: "${ocelot_path}/desktop-light.png"
#lockscreen="/home/username/path/to/lockscreen.jpg"

# natural scrolling on touchpad, recommended if you are used to it
synclient VertScrollDelta=-111

# disable standard rightclick on touchpad, not recommended if you don't own a
# clickable multitouchpad
synclient RightButtonAreaTop=0
synclient RightButtonAreaLeft=0

# set color profile, recommended for blue-ish displays
xcalib -d :0 /usr/share/color/icc/colord/Gamma5500K.icc
